{
  "name": "contacts-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "NODE_ENV=test node --test",
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV nodemon index.js",
    "build:ui": "cd ../contacts && npm install && npm run build && cp -r build ../contacts-server",
    "deploy": "npm run build:ui && npm run start"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mongodb": "^6.9.0",
    "mongoose": "^8.6.3",
    "mongose": "0.0.2-security",
    "morgan": "^1.10.0",
    "nodemon": "^3.1.7"
  }
}
